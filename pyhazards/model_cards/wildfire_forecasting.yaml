model_name: wildfire_forecasting
display_name: Wildfire Forecasting
hazard: Wildfire
include_in_public_catalog: false
catalog_status: hidden
source_file: pyhazards/models/wildfire_forecasting.py
builder_name: wildfire_forecasting_builder
summary: >
  A sequence forecasting baseline for next-window wildfire activity across weekly count features.
description:
  - >
    ``wildfire_forecasting`` is a compact GRU-attention forecaster for weekly wildfire
    activity windows.
  - >
    The PyHazards implementation targets smoke-testable next-window size-group prediction
    through the shared wildfire benchmark flow.
paper:
  title: "Wildfire Danger Prediction and Understanding with Deep Learning"
  url: https://doi.org/10.1029/2022GL099368
  repo_url: https://github.com/Orion-AI-Lab/wildfire_forecasting
tasks:
  - forecasting
  - regression
example: |
  import torch
  from pyhazards.models import build_model

  model = build_model(
      name="wildfire_forecasting",
      task="forecasting",
      input_dim=7,
      output_dim=5,
      lookback=12,
  )
  preds = model(torch.randn(2, 12, 7))
  print(preds.shape)
notes:
  - "Retained as a hidden compatibility entry; it is no longer part of the public 31-model wildfire roster."
smoke_test:
  task: forecasting
  build_kwargs:
    input_dim: 7
    hidden_dim: 32
    output_dim: 5
    lookback: 12
    num_layers: 2
    dropout: 0.0
  input:
    kind: tensor
    tensor:
      shape: [2, 12, 7]
  expected_output:
    kind: tensor
    shape: [2, 5]
